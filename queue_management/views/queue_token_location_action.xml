<?xml version="1.0" encoding="utf-8"?>
<!-- Copyright 2022 CreuBlanca
     License AGPL-3.0 or later (https://www.gnu.org/licenses/agpl). -->

<odoo>

    <record model="ir.ui.view" id="queue_token_location_action_form_view">
        <field name="name">queue_token_location_action.form (in queue_management)</field>
        <field name="model">queue.token.location.action</field>
        <field name="arch" type="xml">
            <form create="0" edit="0" delete="0">
                <header>
                    <!-- TODO -->
                </header>
                <sheet>
                    <!-- TODO -->
                    <group>
                        <field name="token_id"/>

                    </group>
                </sheet>
                <div class="oe_chatter"></div>
            </form>
        </field>
    </record>

    <record model="ir.ui.view" id="queue_token_location_action_search_view">
        <field name="name">queue_token_location_action.search (in queue_management)</field>
        <field name="model">queue.token.location.action</field>
        <field name="arch" type="xml">
            <search>
                <!-- TODO -->
            </search>
        </field>
    </record>

    <record model="ir.ui.view" id="queue_token_location_action_tree_view">
        <field name="name">queue_token_location_action.tree (in queue_management)</field>
        <field name="model">queue.token.location.action</field>
        <field name="arch" type="xml">
            <tree create="0" edit="0" delete="0" default_order="date desc">
                <!-- TODO -->
                <field name="token_id"/>
                <field name="token_location"/>
                <field name="location_id"/>
                <field name="user_id"/>
                <field name="date"/>
                <field name="action"/>
            </tree>
        </field>
    </record>

    <record model="ir.actions.act_window" id="queue_token_location_action_act_window">
        <field name="name">Action Log</field> <!-- TODO -->
        <field name="res_model">queue.token.location.action</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
    </record>

    <record model="ir.ui.menu" id="queue_token_location_action_menu">
        <field name="name">Log</field>
        <field name="parent_id" ref="queue_config_root_menu" />
        <field name="action" ref="queue_token_location_action_act_window" />
        <field name="sequence" eval="50" /> <!-- TODO -->
    </record>



</odoo>
